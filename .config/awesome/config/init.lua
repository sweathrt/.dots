local M = {
    bindings      = require("config.bindings"),
    errors        = require("config.errors"),
    keys          = require("config.keys"),
    menu          = require("config.menu"),
    notifications = require("config.notifications"),
    screens       = require("config.screens"),
    rules         = require("config.rules"),
    tags          = require("config.tags"),
    titlebars     = require("config.titlebars"),
    vars          = require("config.vars"),
    wallpaper     = require("config.wallpaper"),
}

return M
